server:
  port: 50004

spring:
  application:
    name: authorization-server
  cloud:
    nacos:
      discovery:
        server-addr: 139.196.160.33:8848
      config:
        server-addr: 139.196.160.33:8848
        file-extension: yml

feign:
  client:
    config:
      default:
        connectTimeout: 60000
        readTimeout: 60000
